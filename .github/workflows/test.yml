name: test

on:
  push:
  pull_request:

jobs:
  shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: shellcheck -x peon-code.sh install.sh tests/test_peon_code.sh
      - run: tests/test_peon_code.sh
